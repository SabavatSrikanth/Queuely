<%- include('../layouts/main', { title: title + ' - ' + appName, user: typeof user !== 'undefined' ? user : null, body: `
  <div class="d-flex flex-column align-items-center justify-content-center text-center py-5">
    <div class="display-1 fw-bold text-primary mb-3">${statusCode}</div>
    <h3 class="mb-3">${title}</h3>
    <p class="text-muted mb-4" style="max-width: 480px;">${message}</p>
    <a href="/" class="btn btn-primary"><i class="bi bi-house-door me-1"></i> Back to home</a>
  </div>
` }) %>
